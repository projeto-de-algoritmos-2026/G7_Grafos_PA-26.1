{
  "name": "g7_grafos_pa-26.1",
  "version": "1.0.0",
  "description": "Script de automação para iniciar backend e frontend juntos",
  "scripts": {
    "dev:backend": "cd src/backend && ..\\..\\venv\\Scripts\\python.exe -m uvicorn app_setup:app --reload --port 8000",
    "dev:frontend": "cd src/frontend && npm run dev",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "install:all": "npm install && cd src/frontend && npm install && cd ../backend && ..\\..\\venv\\Scripts\\python.exe -m pip install -r requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
